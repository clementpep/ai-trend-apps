{
  "name": "ai-trend-apps",
  "version": "1.0.0",
  "description": "Daily AI-powered applications inspired by viral AI trends",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot hub/src/index.ts",
    "build": "bun build hub/src/index.ts --outdir dist --target bun",
    "start": "bun run dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
